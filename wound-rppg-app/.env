# ST-rPPG Platform — Environment
# Copy to .env and adjust before running docker compose up

# Backend
FLASK_ENV=production
UPLOAD_MAX_MB=10000
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Frontend port
FRONTEND_PORT=3000
BACKEND_PORT=5001
